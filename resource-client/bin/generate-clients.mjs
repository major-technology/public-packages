#!/usr/bin/env node
/**
 * CLI tool to generate singleton resource clients
 * Part of @major-tech/resource-client
 * 
 * Usage:
 *   npx @major-tech/resource-client add <resource_id> <name> <type> <description> <application_id> [--mode app]
 *   npx @major-tech/resource-client add <resource_id> <name> <type> <description> --mode tool
 *   npx @major-tech/resource-client add <resource_id> <name> <type> <description> --mode skill
 *   npx @major-tech/resource-client remove <name> [--mode <app|tool|skill>]
 *   npx @major-tech/resource-client list
 *
 * Modes: app (default) | tool | skill
 * Types: postgresql | clickhouse | mssql | mysql | dynamodb | cosmosdb | snowflake | bigquery | neo4j | hubspot | plaid | linkedin | tiktokads | googlecalendar | gmail | googledrive | googlesheets | outreach | custom | graphql | lambda | salesforce | s3 | slack | majorauth | googleanalytics | quickbooks | gong | clerk | stripe | fireflies | attio | dynamics | linear | zohodesk | zohoprojects | sqs | metamarketing | sharepoint | googlesearchconsole | notion | blob | github
 *
 * Examples:
 *   npx @major-tech/resource-client add "abc-123" "orders-db" "postgresql" "Orders database" "app-123"
 *   npx @major-tech/resource-client add "abc-123" "orders-db" "postgresql" "Orders database" --mode tool
 *   npx @major-tech/resource-client add "xyz-789" "payment-api" "custom" "Payment API" "app-456"
 *   npx @major-tech/resource-client remove "orders-db"
 *   npx @major-tech/resource-client list
 */

import * as fs from 'fs';
import * as path from 'path';
import { fileURLToPath } from 'url';

// Canonical subtype → client-class map. The single source of truth for valid types and the
// typed-vs-proxy decision lives in the package (src/client-registry.ts); we read the built,
// dependency-free copy so codegen doesn't pull the full client bundle.
import { CLIENT_REGISTRY } from '../dist/client-registry.js';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

// User's project root (where the command is run from)
const projectRoot = process.cwd();
const resourcesConfigPath = path.join(projectRoot, 'resources.json');

function getClientsDir() {
  const srcDir = path.join(projectRoot, 'src');

  if (fs.existsSync(srcDir)) {
    return path.join(srcDir, 'clients');
  }

  return path.join(projectRoot, 'clients');
}

/**
 * Client template for app mode
 */
function clientTemplateApp(data, framework) {
  const isNextJs = framework === 'nextjs';

  const imports = [
    `import { ${data.clientClass} } from '@major-tech/resource-client';`,
    isNextJs ? `import { headers } from 'next/headers';` : ''
  ].filter(Boolean).join('\n');

  const getHeadersConfig = isNextJs ? `
        getHeaders: async () => {
          const h = await headers();
          return {
            'x-major-user-jwt': h.get('x-major-user-jwt') || '',
          };
        },` : '';

  return `${imports}

/**
 * ${data.description}
 *
 * Type: ${data.type}
 * Resource ID: ${data.resourceId}
 *
 * DO NOT EDIT - Auto-generated by @major-tech/resource-client
 */

const BASE_URL = ${isNextJs ? 'process.env.MAJOR_API_BASE_URL' : 'import.meta.env.MAJOR_API_BASE_URL'} || 'https://go-api.prod.major.build';
const MAJOR_JWT_TOKEN = ${isNextJs ? 'process.env.MAJOR_JWT_TOKEN' : 'import.meta.env.MAJOR_JWT_TOKEN'};
const APPLICATION_ID = ${isNextJs ? 'process.env.APPLICATION_ID' : 'import.meta.env.APPLICATION_ID'};

class ${data.clientName}Singleton {
  private static instance: ${data.clientClass} | null = null;

  static getInstance(): ${data.clientClass} {
    if (!${data.clientName}Singleton.instance) {
      ${data.clientName}Singleton.instance = new ${data.clientClass}({
        baseUrl: BASE_URL,
        majorJwtToken: MAJOR_JWT_TOKEN,
        applicationId: APPLICATION_ID,
        resourceId: '${data.resourceId}',
        fetch: (...args) => fetch(...args),${getHeadersConfig}
      });
    }
    return ${data.clientName}Singleton.instance;
  }
}

export const ${data.clientName} = ${data.clientName}Singleton.getInstance();
`;
}

/**
 * Client template for tool mode
 */
function clientTemplateTool(data) {
  const factoryName = 'create' + data.clientName.charAt(0).toUpperCase() + data.clientName.slice(1);
  return `import { ${data.clientClass} } from '@major-tech/resource-client';

/**
 * ${data.description}
 *
 * Type: ${data.type}
 * Resource ID: ${data.resourceId}
 * Tool ID: ${data.toolId}
 *
 * DO NOT EDIT - Auto-generated by @major-tech/resource-client (tool mode)
 */

interface ToolContext {
  resourceApiUrl: string;
  majorJwtToken: string;
}

export function ${factoryName}(context: ToolContext): ${data.clientClass} {
  return new ${data.clientClass}({
    baseUrl: context.resourceApiUrl,
    majorJwtToken: context.majorJwtToken,
    toolId: '${data.toolId}',
    resourceId: '${data.resourceId}',
    fetch: (...args: Parameters<typeof fetch>) => fetch(...args),
  });
}
`;
}

/**
 * Client template for skill mode
 *
 * Self-contained singleton that embeds only the resourceId. Identity is resolved
 * at invoke time purely from the deployment-identity JWT (MAJOR_JWT_TOKEN), so
 * there is no applicationId or toolId — the client posts to
 * /internal/skills/v1/resource/:resourceId/invoke.
 */
function clientTemplateSkill(data) {
  return `import { ${data.clientClass} } from '@major-tech/resource-client';

/**
 * ${data.description}
 *
 * Type: ${data.type}
 * Resource ID: ${data.resourceId}
 *
 * DO NOT EDIT - Auto-generated by @major-tech/resource-client (skill mode)
 */

const BASE_URL = process.env.MAJOR_API_BASE_URL || 'https://go-api.prod.major.build';
const MAJOR_JWT_TOKEN = process.env.MAJOR_JWT_TOKEN;

class ${data.clientName}Singleton {
  private static instance: ${data.clientClass} | null = null;

  static getInstance(): ${data.clientClass} {
    if (!${data.clientName}Singleton.instance) {
      ${data.clientName}Singleton.instance = new ${data.clientClass}({
        baseUrl: BASE_URL,
        majorJwtToken: MAJOR_JWT_TOKEN,
        resourceId: '${data.resourceId}',
        fetch: (...args) => fetch(...args),
      });
    }
    return ${data.clientName}Singleton.instance;
  }
}

export const ${data.clientName} = ${data.clientName}Singleton.getInstance();
`;
}

/**
 * Client template dispatcher
 */
function clientTemplate(data, framework, mode) {
  if (mode === 'tool') {
    return clientTemplateTool(data);
  }
  if (mode === 'skill') {
    return clientTemplateSkill(data);
  }
  return clientTemplateApp(data, framework);
}

/**
 * Index template
 */
const RESOURCE_MARKER = 'Auto-generated by @major-tech/resource-client';
const AGENT_MARKER = 'Auto-generated by @major-tech/agents-client';

function collectGeneratedClientExports(clientsDir) {
  if (!fs.existsSync(clientsDir)) {
    return [];
  }

  const exports = [];

  for (const f of fs.readdirSync(clientsDir)) {
    if (!f.endsWith('.ts') || f === 'index.ts' || f === 'bindings.ts') {
      continue;
    }

    const p = path.join(clientsDir, f);

    if (!fs.statSync(p).isFile()) {
      continue;
    }

    const content = fs.readFileSync(p, 'utf-8');

    if (!content.includes(RESOURCE_MARKER) && !content.includes(AGENT_MARKER)) {
      continue;
    }

    const match = content.match(/export (?:const|function) (\w+)/);

    if (!match) {
      continue;
    }

    exports.push({ name: match[1], base: f.replace(/\.ts$/, '') });
  }

  exports.sort((a, b) => a.name.localeCompare(b.name));

  return exports.map((e) => `export { ${e.name} } from './${e.base}';`);
}

function writeMergedIndex(clientsDir) {
  const clientExports = collectGeneratedClientExports(clientsDir);
  const hasBindings = fs.existsSync(path.join(clientsDir, 'bindings.ts'));

  let body;

  if (hasBindings) {
    body = `// DO NOT EDIT - Auto-generated by @major-tech/resource-client
export { getResourceId, getAgentId, getApplicationId, type ResourceSlot, type AgentSlot } from './bindings';

${clientExports.join('\n')}
`;
  } else if (clientExports.length === 0) {
    body = `// No clients configured\n`;
  } else {
    body = `/**
 * Auto-generated client exports
 * DO NOT EDIT - Generated by @major-tech/resource-client
 */

${clientExports.join('\n')}
`;
  }

  fs.writeFileSync(path.join(clientsDir, 'index.ts'), body, 'utf-8');
}

function deleteMarkedFiles(clientsDir, marker, { keepBindings = false } = {}) {
  if (!fs.existsSync(clientsDir)) {
    return;
  }

  for (const f of fs.readdirSync(clientsDir)) {
    if (keepBindings && f === 'bindings.ts') {
      continue;
    }

    const p = path.join(clientsDir, f);

    if (fs.statSync(p).isFile() && fs.readFileSync(p, 'utf-8').includes(marker)) {
      fs.unlinkSync(p);
    }
  }
}

function indexTemplate(data) {
  return `/**
 * Auto-generated client exports
 * DO NOT EDIT - Generated by @major-tech/resource-client
 */

${data.exports.join('\n')}
`;
}

/**
 * Load resources from JSON config file
 */
function loadResources() {
  if (!fs.existsSync(resourcesConfigPath)) {
    return [];
  }
  const content = fs.readFileSync(resourcesConfigPath, 'utf-8');
  return JSON.parse(content);
}

/**
 * Save resources to JSON config file
 */
function saveResources(resources) {
  const dir = path.dirname(resourcesConfigPath);
  if (!fs.existsSync(dir)) {
    fs.mkdirSync(dir, { recursive: true });
  }
  fs.writeFileSync(resourcesConfigPath, JSON.stringify(resources, null, 2), 'utf-8');
}

/**
 * Convert a resource name to a valid camelCase identifier
 * e.g., "yo mama" -> "yoMama", "orders-db" -> "ordersDb"
 */
function toCamelCase(str) {
  return str
    .toLowerCase()
    .replace(/[^a-z0-9]+(.)/g, (_, char) => char.toUpperCase())
    .replace(/[^a-z0-9]+$/, '') // Remove trailing non-alphanumerics
    .replace(/^[^a-z]+/, ''); // Remove leading non-letters
}

// Every subtype with a typed client — derived from the canonical registry, not hand-listed.
const VALID_TYPES = Object.keys(CLIENT_REGISTRY);

// Resolve a subtype to its typed client class name, or null when the subtype has no typed
// client (the bindings generator emits a createProxyFetch handle for those). The `add` path
// validates the type against VALID_TYPES first, so it never sees null here.
function getClientClass(type) {
  return CLIENT_REGISTRY[type] ?? null;
}

function readToolId() {
  const toolJsonPath = path.join(projectRoot, 'tool.json');
  if (!fs.existsSync(toolJsonPath)) {
    console.error('❌ tool.json not found in project root. Run this from your tool directory.');
    process.exit(1);
  }
  const toolJson = JSON.parse(fs.readFileSync(toolJsonPath, 'utf-8'));
  if (!toolJson.id) {
    console.error('❌ tool.json is missing an "id" field.');
    process.exit(1);
  }
  return toolJson.id;
}

function generateClientFile(resource, framework, mode) {
  const clientName = toCamelCase(resource.name) + 'Client';
  const clientClass = getClientClass(resource.type);
  // Each resource stores its own mode (tool/skill); app resources carry an
  // applicationId instead. Fall back to the CLI mode only when neither is set.
  const effectiveMode = resource.mode
    ? resource.mode
    : resource.applicationId
      ? 'app'
      : mode;

  const data = {
    clientClass,
    description: resource.description,
    type: resource.type,
    resourceId: resource.id,
    clientName,
  };

  if (effectiveMode === 'tool') {
    data.toolId = readToolId();
  }

  return clientTemplate(data, framework, effectiveMode);
}

function generateIndexFile(resources) {
  if (resources.length === 0) {
    return `// No clients configured\n`;
  }

  const exports = resources.map(resource => {
    const clientName = toCamelCase(resource.name) + 'Client';
    const fileName = toCamelCase(resource.name);
    if (resource.mode === 'tool') {
      const factoryName = 'create' + clientName.charAt(0).toUpperCase() + clientName.slice(1);
      return `export { ${factoryName} } from './${fileName}';`;
    }
    return `export { ${clientName} } from './${fileName}';`;
  });

  return indexTemplate({ exports });
}

function addResource(resourceId, name, type, description, applicationId, framework, mode) {
  if (!VALID_TYPES.includes(type)) {
    console.error(`❌ Invalid type: ${type}`);
    console.error(`   Valid types: ${VALID_TYPES.join(', ')}`);
    console.error(`   Your resource-client version might be out of date. Try running 'pnpm update @major-tech/resource-client'`)
    process.exit(1);
  }

  if (mode === 'app' && !applicationId) {
    console.error('❌ Application ID is required');
    process.exit(1);
  }

  const resources = loadResources();

  const existing = resources.find(r => r.name === name);
  if (existing) {
    console.error(`❌ Resource with name "${name}" already exists`);
    process.exit(1);
  }

  const newResource = {
    id: resourceId,
    name,
    type,
    description,
    ...(mode === 'tool' || mode === 'skill' ? { mode } : { applicationId }),
  };

  resources.push(newResource);
  saveResources(resources);

  console.log(`✅ Added resource: ${name}`);
  console.log(`   Type: ${type}`);
  console.log(`   ID: ${resourceId}`);

  regenerateClients(resources, framework, mode);
}

/**
 * Remove a resource by name
 */
function removeResource(name, framework, mode) {
  const resources = loadResources();
  const index = resources.findIndex(r => r.name === name);

  if (index === -1) {
    console.error(`❌ Resource "${name}" not found`);
    process.exit(1);
  }

  const removed = resources.splice(index, 1)[0];
  saveResources(resources);

  console.log(`✅ Removed resource: ${removed.name}`);
  console.log(`   ID: ${removed.id}`);

  regenerateClients(resources, framework, mode);
}

/**
 * List all resources
 */
function listResources() {
  const resources = loadResources();

  if (resources.length === 0) {
    console.log('No resources configured');
    return;
  }

  console.log(`Resources (${resources.length}):\n`);
  resources.forEach((r, idx) => {
    console.log(`${idx + 1}. ${r.name} (${r.type})`);
    console.log(`   ID: ${r.id}`);
    console.log(`   Client: ${toCamelCase(r.name)}Client`);
    console.log('');
  });
}

/**
 * Regenerate all client files
 */
function regenerateClients(resources, framework, mode) {
  const clientsDir = getClientsDir();

  // Ensure clients directory exists
  if (!fs.existsSync(clientsDir)) {
    fs.mkdirSync(clientsDir, { recursive: true });
  }

  // Remove only files WE generated — leave agent-client files (and any
  // author-written modules) in place so a resource add/remove doesn't clobber them.
  deleteMarkedFiles(clientsDir, RESOURCE_MARKER, { keepBindings: true });

  // Generate individual client files
  resources.forEach(resource => {
    const fileName = toCamelCase(resource.name) + '.ts';
    const filePath = path.join(clientsDir, fileName);
    const code = generateClientFile(resource, framework, mode);
    fs.writeFileSync(filePath, code, 'utf-8');
  });

  writeMergedIndex(clientsDir);

  console.log(`✅ Generated ${resources.length} client(s) in ${clientsDir}`);
  if (framework) {
    console.log(`   Framework: ${framework}`);
  }
}

// ---------------------------------------------------------------------------
// bindings.json-driven generation (slots model). Reads `bindings.json` at the
// project root and emits one slot-addressed client per resource slot — typed
// (`new XClient({ resourceId: getResourceId("SLOT") })`) or, for a resource with
// no typed client, a `createProxyFetch` handle. NO ids are baked; each client resolves
// its id from the app's committed bindings.json via the generated `clients/bindings.ts`
// (typed, self-contained — so `getResourceId("typo")` is a compile error, with no global
// registration and no import-order hazard).
// ---------------------------------------------------------------------------

function loadBindings() {
  const p = path.join(projectRoot, 'bindings.json');

  if (!fs.existsSync(p)) {
    console.error('❌ bindings.json not found in project root.');
    process.exit(1);
  }

  return JSON.parse(fs.readFileSync(p, 'utf-8'));
}

const slotClientName = (slot) => toCamelCase(slot.toLowerCase()) + 'Client';
const slotFileBase = (slot) => toCamelCase(slot.toLowerCase()) || 'resource';

function envReads(framework) {
  const isNextJs = framework === 'nextjs';
  const root = isNextJs ? 'process.env' : 'import.meta.env';

  return { base: `${root}.MAJOR_API_BASE_URL`, jwt: `${root}.MAJOR_JWT_TOKEN`, isNextJs };
}

function typedSlotClientTemplate(slot, type, clientClass, framework) {
  const { base, jwt, isNextJs } = envReads(framework);
  const headerImport = isNextJs ? `\nimport { headers } from 'next/headers';` : '';
  const getHeaders = isNextJs
    ? `
  getHeaders: async () => {
    const h = await headers();
    return { 'x-major-user-jwt': h.get('x-major-user-jwt') || '' };
  },`
    : '';

  // Server-only: this singleton holds MAJOR_JWT_TOKEN and calls getApplicationId() at module load, so
  // pulling it into a client bundle must be a build error — not the "APPLICATION_ID is not set" crash.
  const serverOnly = isNextJs ? "import 'server-only';\n" : '';

  return `${serverOnly}import { getResourceId, getApplicationId } from './bindings';
import { ${clientClass} } from '@major-tech/resource-client';${headerImport}

/**
 * Resource slot: ${slot} (type: ${type})
 * DO NOT EDIT - Auto-generated by @major-tech/resource-client
 */
const BASE_URL = ${base} || 'https://go-api.prod.major.build';
const MAJOR_JWT_TOKEN = ${jwt};

export const ${slotClientName(slot)} = new ${clientClass}({
  baseUrl: BASE_URL,
  majorJwtToken: MAJOR_JWT_TOKEN,
  applicationId: getApplicationId(),
  resourceId: getResourceId('${slot}'),
  fetch: (...args) => fetch(...args),${getHeaders}
});
`;
}

function proxySlotClientTemplate(slot, framework) {
  const { base, jwt, isNextJs } = envReads(framework);
  // Mirror the typed client: server-only + the next/headers-backed proxy for Next; the
  // framework-agnostic core proxy (explicit JWT) for vite/others. Emitting `server-only`
  // or `/next` unconditionally broke non-Next apps that had a proxy slot.
  const serverOnly = isNextJs ? "import 'server-only';\n" : '';
  const proxyImport = isNextJs
    ? `import { createProxyFetch } from '@major-tech/resource-client/next';`
    : `import { createProxyFetch } from '@major-tech/resource-client';`;

  return `${serverOnly}import { getResourceId } from './bindings';
${proxyImport}

/**
 * HTTP-proxy resource slot: ${slot}
 * DO NOT EDIT - Auto-generated by @major-tech/resource-client
 */
const BASE_URL = ${base} || 'https://go-api.prod.major.build';
const MAJOR_JWT_TOKEN = ${jwt};

export const ${slotClientName(slot)} = createProxyFetch({
  baseUrl: BASE_URL,
  resourceId: getResourceId('${slot}'),
  majorJwtToken: MAJOR_JWT_TOKEN,
});
`;
}

// The readers module: imports the app's committed bindings.json, registers it with the package, and
// re-exports the readers narrowed to this app's slots. Every generated client imports `getResourceId`
// from HERE — a real value import — so this module (and therefore the registration) is guaranteed to
// have evaluated before any slot is resolved. App code must import from here too: taking the readers
// straight from the package bypasses this module, so nothing has registered and they throw.
// There is deliberately no `_register` side-effect module; that shape could be reached without it.
function bindingsReadersTemplate(resourceSlots, agentSlots, relBindingsPath) {
  const union = (slots) => (slots.length ? slots.map((s) => `'${s}'`).join(' | ') : 'never');

  return `// DO NOT EDIT - Auto-generated by @major-tech/resource-client
import bindingsJson from '${relBindingsPath}';
import {
  registerBindings,
  getResourceId as readResourceId,
  getAgentId as readAgentId,
  getApplicationId,
  type BindingsManifest,
} from '@major-tech/resource-client';

// Registered at module scope, before anything below can be used. Every generated client imports its
// readers from THIS module (a real value import), so the bundler cannot drop or reorder the
// dependency and this registration is guaranteed to have run before any slot is resolved. Import
// readers from './clients' in app code too — importing them straight from the package skips this
// module, and they will throw saying so.
registerBindings(bindingsJson as BindingsManifest);

export type ResourceSlot = ${union(resourceSlots)};
export type AgentSlot = ${union(agentSlots)};

/** Resolve a resource slot to its bound id (compile-checked against this app's slots). */
export const getResourceId = (slot: ResourceSlot): string => readResourceId(slot);
/** Resolve an agent slot to its bound id (compile-checked against this app's slots). */
export const getAgentId = (slot: AgentSlot): string => readAgentId(slot);
export { getApplicationId };
`;
}

function bindingsIndexTemplate(clientExports) {
  return `// DO NOT EDIT - Auto-generated by @major-tech/resource-client
export { getResourceId, getAgentId, getApplicationId, type ResourceSlot, type AgentSlot } from './bindings';

${clientExports.join('\n')}
`;
}

function generateFromBindings(framework) {
  const bindings = loadBindings();
  const resources = bindings?.slots?.resources ?? {};
  const agents = bindings?.slots?.agents ?? {};
  const clientsDir = getClientsDir();

  if (!fs.existsSync(clientsDir)) {
    fs.mkdirSync(clientsDir, { recursive: true });
  }

  // Remove only files WE generated (they carry the marker) — a regen drops clients for
  // slots that no longer exist without clobbering agent-client files or any module the
  // author hand-wrote here.
  deleteMarkedFiles(clientsDir, RESOURCE_MARKER);

  const relBindings =
    './' + path.relative(clientsDir, path.join(projectRoot, 'bindings.json')).split(path.sep).join('/');
  fs.writeFileSync(
    path.join(clientsDir, 'bindings.ts'),
    bindingsReadersTemplate(Object.keys(resources), Object.keys(agents), relBindings),
    'utf-8',
  );

  for (const [slot, entry] of Object.entries(resources)) {
    const type = entry.type || '';
    const clientClass = getClientClass(type);
    const code = clientClass
      ? typedSlotClientTemplate(slot, type, clientClass, framework)
      : proxySlotClientTemplate(slot, framework);

    fs.writeFileSync(path.join(clientsDir, slotFileBase(slot) + '.ts'), code, 'utf-8');
  }

  writeMergedIndex(clientsDir);

  console.log(`✅ Generated ${Object.keys(resources).length} resource client(s) + typed readers in ${clientsDir}`);
}

/**
 * Main execution
 */
function main() {
  const args = process.argv.slice(2);
  const command = args[0];

  // Extract --framework flag
  const frameworkIndex = args.indexOf('--framework');
  let framework = undefined;
  if (frameworkIndex !== -1 && args[frameworkIndex + 1]) {
    framework = args[frameworkIndex + 1];
    // Remove --framework and its value from args
    args.splice(frameworkIndex, 2);
  }

  // Extract --mode flag (app | tool, default: app)
  const modeIndex = args.indexOf('--mode');
  let mode = 'app';
  if (modeIndex !== -1 && args[modeIndex + 1]) {
    mode = args[modeIndex + 1];
    // Remove --mode and its value from args
    args.splice(modeIndex, 2);
  }

  if (mode !== 'app' && mode !== 'tool' && mode !== 'skill') {
    console.error(`❌ Invalid mode: ${mode}. Must be "app", "tool", or "skill".`);
    process.exit(1);
  }

  if (!command) {
    console.log('Usage:');
    console.log('  npx @major-tech/resource-client add <resource_id> <name> <type> <description> <application_id> [--framework <nextjs|vite>] [--mode <app|tool|skill>]');
    console.log('  npx @major-tech/resource-client add <resource_id> <name> <type> <description> --mode tool');
    console.log('  npx @major-tech/resource-client add <resource_id> <name> <type> <description> --mode skill');
    console.log('  npx @major-tech/resource-client remove <name> [--framework <nextjs|vite>] [--mode <app|tool|skill>]');
    console.log('  npx @major-tech/resource-client list');
    console.log('\nModes: app (default) | tool | skill');
    console.log('  app   — requires <application_id>, reads MAJOR_API_BASE_URL');
    console.log('  tool  — embeds toolId from tool.json at generation time, reads RESOURCE_API_URL');
    console.log('  skill — embeds only resourceId; identity comes from the deployment-identity JWT, reads MAJOR_API_BASE_URL + MAJOR_JWT_TOKEN');
    console.log(`\nTypes: ${VALID_TYPES.join(' | ')}`);
    return;
  }

  switch (command) {
    case 'add': {
      if (mode === 'tool' || mode === 'skill') {
        const [, resourceId, name, type, description] = args;
        if (!resourceId || !name || !type || description === undefined) {
          console.error('❌ Missing arguments');
          console.log(`Usage: add <resource_id> <name> <type> <description> --mode ${mode}`);
          process.exit(1);
        }
        addResource(resourceId, name, type, description, null, framework, mode);
      } else {
        const [, resourceId, name, type, description, applicationId] = args;
        if (!resourceId || !name || !type || description === undefined || !applicationId) {
          console.error('❌ Missing arguments');
          console.log('Usage: add <resource_id> <name> <type> <description> <application_id> [--framework <nextjs|vite>]');
          process.exit(1);
        }
        addResource(resourceId, name, type, description, applicationId, framework, mode);
      }
      break;
    }

    case 'remove': {
      const [, name] = args;
      if (!name) {
        console.error('❌ Missing name');
        process.exit(1);
      }
      removeResource(name, framework, mode);
      break;
    }

    case 'list': {
      listResources();
      break;
    }

    case 'generate': {
      // Slots model: regenerate all clients + typed readers from the project's bindings.json.
      generateFromBindings(framework);
      break;
    }

    default: {
      console.error(`❌ Unknown command: ${command}`);
      process.exit(1);
    }
  }
}

main();
